namespace Misc
{
    public class FigureMeta
    {
        public FigureType type;
        public FigureColor color;

        public FigureMeta(FigureType type, FigureColor color)
        {
            this.type = type;
            this.color = color;
        }
    }
}
